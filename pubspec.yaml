name: meet6
description: Meet6 - 6 kisilik canli tanisma odalari.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  crop_your_image: ^2.0.0
  cupertino_icons: ^1.0.8
  firebase_analytics: ^12.5.0
  firebase_core: ^4.14.0
  firebase_crashlytics: ^5.3.0
  firebase_messaging: ^16.6.0
  flutter_image_compress: ^2.5.1
  geolocator: ^13.0.2
  http: ^1.2.2
  http_parser: ^4.1.2
  image_picker: ^1.1.2
  purchases_flutter: ^10.11.0
  shared_preferences: ^2.3.3
  socket_io_client: ^3.1.2
  video_player: ^2.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
